{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "keyword-index-service.fullname" . }}
  labels:
    {{- include "keyword-index-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- toYaml .Values.secrets | nindent 2 }}
{{- end }}
