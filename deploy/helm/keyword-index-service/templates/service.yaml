apiVersion: v1
kind: Service
metadata:
  name: {{ include "keyword-index-service.fullname" . }}
  labels:
    {{- include "keyword-index-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "keyword-index-service.selectorLabels" . | nindent 4 }}
