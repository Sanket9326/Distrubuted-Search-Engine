apiVersion: v1
kind: Service
metadata:
  name: {{ include "reliability-service.fullname" . }}
  labels:
    {{- include "reliability-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "reliability-service.selectorLabels" . | nindent 4 }}
